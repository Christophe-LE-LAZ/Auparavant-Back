{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'memory.title.new'|trans }}</h1>

    {{ include('back/memory/_form.html.twig') }}

    <a href="{{ path('app_memory_index') }}">{{ 'button.back'|trans }}</a>
{% endblock %}
