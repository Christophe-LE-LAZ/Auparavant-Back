{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ 'place.title.new'|trans }}</h1>

    {{ include('back/place/_form.html.twig') }}

    <a href="{{ path('app_place_index') }}">{{ 'button.back'|trans }}</a>
{% endblock %}
