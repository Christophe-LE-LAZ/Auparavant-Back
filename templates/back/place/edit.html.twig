<!DOCTYPE html>

<title>Modifier l'endroit</title>

{% block body %}
    <h1>Modifier l'endroit</h1>

    {{ include('back/place/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_place_index') }}">retour à la liste</a>

    {{ include('back/place/_delete_form.html.twig') }}
{% endblock %}
