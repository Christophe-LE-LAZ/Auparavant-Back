{# {% extends 'base.html.twig' %}

{% block body %}
    <h1 class="m-5  text-center">Modifier une localité</h1>

    <div class="container-fluid">
        <div class="row">
            <div class="col">
                {{ include('back/location/_form.html.twig', {'button_label': 'Valider'}) }}
                {{ include('back/location/_delete_form.html.twig') }}
            </div>
        </div>
    </div>
{% endblock %} #}

{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="mb-5 mx-3">Modifier une localité</h1>

    <div class="container-fluid">
        <div class="row">
            <div class="col">
                <div class="form-group">
                    {{ include('back/location/_form.html.twig', {'button_label': 'Valider'}) }}
                    <div class="col-1">
			{{ include('back/location/_delete_form.html.twig') }}
		</div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
