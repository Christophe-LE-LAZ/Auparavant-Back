{% extends 'base.html.twig' %}

{% block body %}
	<h1 class="mx-3 mb-5">{{ 'location.title'|trans }}</h1>

	<table class="table">
		<tbody>
            <tr>
                <th>Id</th>
                <td>{{ location.id }}</td>
            </tr>
            <tr>
                <th>{{ 'location.area'|trans }}</th>
                <td>{{ location.area }}</td>
            </tr>
            <tr>
                <th>{{ 'location.department'|trans }}</th>
                <td>{{ location.department }}</td>
            </tr>
            <tr>
                <th>{{ 'location.district'|trans }}</th>
                <td>{{ location.district }}</td>
            </tr>
            <tr>
                <th>{{ 'location.street'|trans }}</th>
                <td>{{ location.street }}</td>
            </tr>
            <tr>
                <th>{{ 'location.zipcode'|trans }}</th>
                <td>{{ location.zipcode }}</td>
            </tr>
            <tr>
                <th>{{ 'location.street'|trans }}</th>
                <td>{{ location.city }}</td>
            </tr>
            <tr>
                <th>{{ 'location.latitude'|trans }}</th>
                <td>{{ location.latitude }}</td>
            </tr>
            <tr>
                <th>{{ 'location.longitude'|trans }}</th>
                <td>{{ location.longitude }}</td>
            </tr>
        </tbody>
	</table>
	    <div class="row">

		    <div class="col d-flex flex-column flex-md-row mx-3 my-2 align-items-md-start">
			    <a class="btn btn-lg btn-secondary text-white d-inline-block mb-2 mx-md-3" href="{{ path('app_location_index') }}">
                    {# Cell phone #}
                    {# <div class="d-block d-sm-none"><i class="bi bi-backspace-fill"></i></div> #}
                    {# Else #}
                    <div class="d-block">{{ 'button.back'|trans }}</div>
                </a>
            
			    <a class="btn btn-lg btn-warning text-white" href="{{ path('app_location_edit', {'id': location.id}) }}">
                    {# Cell phone #}
                    {# <div class="d-block d-sm-none"><i class="bi bi-pencil-fill"></i></div> #}
                    {# Else #}
                    <div class="d-block">{{ 'button.edit'|trans }}</div>
                </a>
		    </div>
        
        </div>
        
		    <div class="px-3 col col-md-2 d-md-inline-block div-btn-delete">
			    {{ include('back/location/_delete_form.html.twig') }}
		    </div>
	    
{% endblock %}
