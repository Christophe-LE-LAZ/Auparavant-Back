{% extends 'base.html.twig' %}

{% block body %}
	<h1 class="mx-3 mb-5">{{ 'location.title'|trans }}</h1>

	<table class="table">
		<tbody>
            <tr>
                <th>Id</th>
                <td>{{ location.id }}</td>
            </tr>
            <tr>
                <th>{{ 'location.area'|trans }}</th>
                <td>{{ location.area }}</td>
            </tr>
            <tr>
                <th>{{ 'location.department'|trans }}</th>
                <td>{{ location.department }}</td>
            </tr>
            <tr>
                <th>{{ 'location.district'|trans }}</th>
                <td>{{ location.district }}</td>
            </tr>
            <tr>
                <th>{{ 'location.street'|trans }}</th>
                <td>{{ location.street }}</td>
            </tr>
            <tr>
                <th>{{ 'location.zipcode'|trans }}</th>
                <td>{{ location.zipcode }}</td>
            </tr>
            <tr>
                <th>{{ 'location.street'|trans }}</th>
                <td>{{ location.city }}</td>
            </tr>
            <tr>
                <th>{{ 'location.latitude'|trans }}</th>
                <td>{{ location.latitude }}</td>
            </tr>
            <tr>
                <th>{{ 'location.longitude'|trans }}</th>
                <td>{{ location.longitude }}</td>
            </tr>
        </tbody>
	</table>
    {# {% if is_mobile %} #}
        {# <div class="row">

            <div class="ms-5 col-2">
                <a class="btn btn-lg btn-secondary text-white d-inline-block" href="{{ path('app_location_index') }}"><i class="bi bi-backspace-fill"></i></a>
            </div>

            <div class="col-1">
                <a class="btn btn-lg btn-success" href="{{ path('app_location_edit', {'id': location.id}) }}"><i class="bi bi-pencil-fill"></i></a>
            </div>

            <div class="col-1">
                {{ include('back/location/_delete_form.html.twig') }}
            </div>

        </div>  #}
    {# {% else %} #}
	    <div class="row">

		    <div class="ms-5 col-2">
			    <a class="btn btn-lg btn-secondary text-white d-inline-block" href="{{ path('app_location_index') }}">{{ 'button.back'|trans }}</a>
		    </div>

		    <div class="col-1">
			    <a class="btn btn-lg btn-success" href="{{ path('app_location_edit', {'id': location.id}) }}">{{ 'button.edit'|trans }}</a>
		    </div>

		    <div class="col-1">
			    {{ include('back/location/_delete_form.html.twig') }}
		    </div>
    
	    </div>
    {# {% endif %} #}
{% endblock %}
