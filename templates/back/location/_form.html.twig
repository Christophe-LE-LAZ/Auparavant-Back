<div class="form-group">
	{{ form_start(form) }}
		{{ form_widget(form) }}
			<div class="row">
				<div class="col d-flex flex-column flex-md-row mx-md-3 my-md-2 align-items-md-start">
					<button class="btn btn-lg btn-success text-white mb-2">
						{# {% if is_mobile %}
						{{ button_label|default(' '|trans) }}<i class="bi bi-check-square-fill"></i>
						{% else %} #}
						{{ button_label|default('button.submit'|trans) }}
						{# {% else %} #}
					</button>
					<a class="btn btn-lg btn-secondary text-white mb-2 mx-md-2" href="{{ path('app_location_index') }}">
						{# {% if is_mobile %}
						<i class="bi bi-backspace-fill"></i>
						{% else %} #}
						{{ 'button.back'|trans }}
						{# {% else %} #}
					</a>
				</div>
			</div>
	{{ form_end(form) }}
</div>

