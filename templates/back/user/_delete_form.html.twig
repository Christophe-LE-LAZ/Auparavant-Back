<form method="post" action="{{ path('app_user_delete', {'id': user.id}) }}" onsubmit="return confirm('Ê{{ 'deletion_confirmation'|trans }}');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-lg text-white mb-2 btn-danger">
        {# Cell phone #}
        <div class="d-block d-sm-none"><i class="bi bi-trash3-fill"></i></div>
        {# Else #}
        <div class="d-none d-sm-block">{{ 'button.delete'|trans }}</div>
    </button>
</form>

