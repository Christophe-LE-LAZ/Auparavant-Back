{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="mb-5 mx-3">{{ 'picture.title.edit'|trans }}</h1>
    <div class="container-fluid">
        <div class="row">
            <div class="col">
                <div class="form-group">
                    {# {% if is_mobile %}
                    {{ include('back/picture/_form.html.twig', {'button_label': ' '|trans}) }}
                    {% else %} #}
                    {{ include('back/picture/_form.html.twig', {'button_label': 'button.update'|trans}) }}
                    {# {% endif %} #}

                    <div class="col col-md-2 d-md-inline-block div-btn-delete">
                        {{ include('back/picture/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
