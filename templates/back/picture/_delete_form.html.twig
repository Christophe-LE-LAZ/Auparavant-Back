{# {% if is_mobile %} #} 
    {# <form method="post" action="{{ path('app_picture_delete', {'id': picture.id}) }}" onsubmit="return confirm('{{ 'deletion_confirmation'|trans }}');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picture.id) }}">
        <button class="btn btn-lg text-white mb-2 btn-danger"><i class="bi bi-trash3-fill"></i></button>
    </form> #}
{# {% else %} #}
    <form method="post" action="{{ path('app_picture_delete', {'id': picture.id}) }}" onsubmit="return confirm('{{ 'deletion_confirmation'|trans }}');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ picture.id) }}">
        <button class="btn btn-lg text-white mb-2 btn-danger">{{ 'button.delete'|trans }}</button>
    </form>
{# {% endif %} #}